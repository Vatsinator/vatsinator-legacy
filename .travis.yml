language: cpp

compiler:
    - clang

before_install:
    - brew update
    - brew install qt5

before_script:
    - cmake . -DCMAKE_PREFIX_PATH=/usr/local/opt/qt5/lib/cmake

script:
    - make VERBOSE=1
    - make package

os:
    - osx
