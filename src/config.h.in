#ifndef VATSINATOR_CONFIG_H
#define VATSINATOR_CONFIG_H

#define VATSINATOR_VERSION "${vatsinator_VERSION}"
#define VATSINATOR_PREFIX "${vatsinator_PREFIX}"

#define VATSINATOR_SOURCE_PATH "${vatsinator_SOURCE_PATH}"
#define VATSINATOR_I18N_PATH "${CMAKE_CURRENT_BINARY_DIR}/i18n"

#endif // VATSINATOR_CONFIG_H
